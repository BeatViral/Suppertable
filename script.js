const toggle=document.querySelector('.menu-toggle');
const nav=document.querySelector('.site-nav');
if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach((link)=>link.addEventListener('click',()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false');}));}

const applicationForm=document.querySelector('#founding-application');
if(applicationForm){const photoInput=applicationForm.elements.foodPhotos;photoInput.addEventListener('change',()=>{photoInput.setCustomValidity(photoInput.files.length>3?'Please choose no more than three food photos.':'');});applicationForm.addEventListener('submit',(event)=>{event.preventDefault();if(!applicationForm.reportValidity())return;const data=new FormData(applicationForm);const photos=[...photoInput.files].map((file)=>file.name);const fields=[['Name','name'],['Email or mobile','contact'],['Suburb','suburb'],['Food type','foodType'],['Signature dishes','signatureDishes'],['Current kitchen','currentKitchen'],['Kitchen name','kitchenName'],['Sold food before','soldBefore'],['Why launch','whyLaunch']];const details=fields.map(([label,key])=>`${label}: ${data.get(key)||'Not provided'}`).join('\n');const photoLine=photos.length?`\n\nFood photos selected: ${photos.join(', ')}\nPlease attach these photos to this email before sending.`:'';const subject=`Founding Kitchen Application — ${data.get('name')}`;window.location.href=`mailto:ssuaustralia@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(`Founding Kitchen application\n\n${details}${photoLine}`)}`;const success=document.querySelector('#application-success');success.hidden=false;success.focus();});}
